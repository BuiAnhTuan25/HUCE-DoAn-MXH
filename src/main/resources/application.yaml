server:
  port: 8081

spring:
  application:
    name: social-service
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
    password: 123456

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: false
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: false
        default_schema: social
      jakarta:
        persistence:
          create-database-schemas: false

  mvc:
    pathmatch:
      matching-strategy: ant-path-matcher

  mail:
    host: smtp.gmail.com
    port: 587
    username: tuantk7x@gmail.com
    password: gcgothibughkqign
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

  security:
    oauth2:
      client:
        registration:
          facebook:
            client-id: ${FACEBOOK_CLIENT_ID:744716950059120}
            client-secret: ${FACEBOOK_CLIENT_SECRET}
            scope: email, public_profile
          google:
            client-id: ${GOOGLE_CLIENT_ID:716469277461-al1ds3lmgi6r7s9lm5ggs2idaqfpjp6r.apps.googleusercontent.com}
            client-secret: ${GOOGLE_CLIENT_SECRET}
            scope: email, profile

jwt:
  secret: mxhhuce
  expiration: 86400000

logging:
  level:
    org.hibernate: ERROR